import type { RendererProps } from "./types";

/** An info screen has no control: its text and help are rendered by QuestionScreen like every other type. */
export function Info(_: RendererProps) {
  return null;
}
