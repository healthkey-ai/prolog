# Theming the runner

Moved to the [theme manual](definitions/theme-definition.md) — the complete reference for
`schema/theme.schema.json`: theme directory layout, every field, token roles,
chrome-string keys, registration rules (assets, contrast), mounting, and how
the runner applies a theme at runtime.

See also the [survey definition manual](definitions/survey-definition.md) for the
`theme` field that selects a theme per survey.
